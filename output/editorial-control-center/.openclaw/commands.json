{
  "commands": {
    "test": "npm test",
    "lint": "npm run lint",
    "build": "npm run build",
    "typecheck": "npm run typecheck",
    "dev": "npm run dev",
    "db_start": "docker compose up -d postgres",
    "db_stop": "docker compose down",
    "db_migrate": "npm run db:migrate"
  },
  "setup": {
    "install": "npm install",
    "env": "test -f .env.local || cp .env.example .env.local",
    "db_start": "docker compose up -d postgres",
    "db_migrate": "npm run db:migrate"
  },
  "notes": [
    "Commands and validation contract detected from package.json."
  ],
  "validation": {
    "ecosystem": "node",
    "test_runner": "vitest",
    "requires_real_tests": true,
    "block_on": [
      "test",
      "build",
      "typecheck"
    ],
    "warn_on": [
      "lint"
    ]
  }
}
